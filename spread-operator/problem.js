const defaultSettings = {
  theme: 'light',
  language: 'de',
  notifications: true
};

const userOverrides = {
  theme: 'dark'
};

/*
 * TODO: Erstelle ein neues Objekt finalSettings, das:
 *
 * - alle Eigenschaften aus defaultSettings enthält
 * - durch userOverrides überschrieben wird
 *
 * 👉 Verwende Spread, nicht manuelle Zuweisungen.
 */

const scores = [10, 20, 30];

/*
 * TODO: Erstelle ein neues Array extendedScores, das:
 *
 * - vorne den Wert 5 enthält
 * - danach alle Werte aus scores
 * - hinten den Wert 40
 */
